If you want to use web browser detect language you should remove commentmarks in index.js on
// .use(middleware.LanguageDetector)

But remember that in database the fish is hardcoded as Pike, Perch and Zander.
If you remove // from // .use(middleware.LanguageDetector) it will search in your language instead of English.
